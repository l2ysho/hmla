Recessed monospace text field. Pass `label` for the uppercase silkscreen label, or omit for a bare input.

```jsx
<Input label="seed" defaultValue="0x4F2A" />
<Input placeholder="search presets…" />
```

Inherits all native input props (`type`, `value`, `onChange`, `placeholder`, `disabled`). Focus gains an accent ring + glow.
