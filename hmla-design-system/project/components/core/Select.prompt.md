Recessed monospace dropdown. Pass `options` (strings or `{value,label}`) or raw `<option>` children.

```jsx
<Select label="output bus" options={["A · main", "B · cans", "C · send"]} />
```

Optional `label`; inherits native select props (`value`, `onChange`, `disabled`).
