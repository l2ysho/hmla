Glowing indicator dot for hardware status. `tone` from the signal palette; `on` toggles the glow.

```jsx
<LED tone="green" />            // armed
<LED tone="red" on={clipping} /> // clip
<LED tone="blue" /> sync
```
