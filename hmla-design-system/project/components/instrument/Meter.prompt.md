Level meter with a green→amber→red gradient. Drive `value` (0..1) from real signal level.

```jsx
<Meter value={0.72} />
<Meter orient="v" value={rms} style={{ height: 120 }} />
```

Set width (horizontal) or height (vertical) via `style`.
